<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F5F5F5">

    <ImageView
        android:id="@+id/backButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Bouton retour"
        android:layout_margin="16dp"
        android:onClick="onBackPressed" />

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Assistant de Santé IA"
        android:textSize="24sp"
        android:gravity="center"
        android:padding="16dp"
        android:textColor="#FFFFFF"
        android:background="#4491f9"
        android:layout_marginBottom="16dp"
        android:elevation="4dp"
        android:fontFamily="sans-serif-medium" />

    <!-- Zone des réponses -->
    <TextView
        android:id="@+id/textViewResponse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Bienvenue, comment puis-je vous aider aujourd'hui ?"
        android:textSize="18sp"
        android:padding="12dp"
        android:layout_marginBottom="16dp"
        android:background="#E0F2FF"
        android:textColor="#000000"
        android:elevation="2dp"
        android:fontFamily="sans-serif-light" />

    <!-- Zone de saisie utilisateur -->
    <EditText
        android:id="@+id/editTextMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Décrivez vos symptômes ici..."
        android:padding="12dp"
        android:background="#FFFFFF"
        android:layout_marginBottom="12dp"
        android:textColor="#000000"
        android:fontFamily="sans-serif"
        android:inputType="textMultiLine" />

    <!-- Bouton d'envoi -->
    <Button
        android:id="@+id/buttonSend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Envoyer"
        android:backgroundTint="#4491f9"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:padding="12dp"
        android:fontFamily="sans-serif-medium" />

</LinearLayout>
